{
  "name": "trl-lead-os",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first lead workspace",
  "scripts": {
    "start": "python3 -m http.server 3000 --bind 0.0.0.0",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
